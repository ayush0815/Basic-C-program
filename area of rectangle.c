#include <stdio.h>

int main()
{
int a,b,area;
printf("give the length of rectangle\n");
scanf("%d",&a);
printf("give the breadth of the rectangle\n");
scanf("%d",&b);
    area=a*b;
    printf("the area of rectangle is %d",area);
}